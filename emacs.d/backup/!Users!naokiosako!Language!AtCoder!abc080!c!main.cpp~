#include<bits/stdc++.h>
using namespace std;
#define ll long long int

int main() {
    int N, ans=-1e9;
    cin >> N;
    int F[N][10], P[N][10];

    for(int i=0; i<N; i++){
	for(int j=0; j<10; j++){
	    cin >> F[i][j];
	}
    }

    for(int i=0; i<N; i++){
	for(int j=0; j<10; j++){
	    cin >> P[i][j];
	}
    }

    // for(int k=1; k<1024; k++){
    // 	int tmp=k;
	
    // 	// for(int i=0; i<N; i++){
    // 	//     for(int j=0; j<10; j++){
    // 	// 	if(tmp&1)
    // 	// 	    cin >> P[i][j];
    // 	// 	tmp>>1;
    // 	//     }
    // 	// }
    // }

    







    cout << ans << endl;
} 



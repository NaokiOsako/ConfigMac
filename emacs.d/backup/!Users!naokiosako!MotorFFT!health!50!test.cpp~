#include<bits/stdc++.h>
using namespace std;

#define SAMPLE 513 // sample
int main(){
    double maxDB=-1e9;
    double DB;

    for(int i=0; i<SAMPLE; i++){
	cin >> DB;
	if(i>=5 && maxDB < DB)
	    maxDB = DB;
    }
    printf("%.1f\n", maxDB);
}

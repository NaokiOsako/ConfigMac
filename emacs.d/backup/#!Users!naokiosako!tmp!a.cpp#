
sort(v.begin(), v.end());
sort(v.begin(), v.end(), greater<int>());
for(int i=0; i<N; i++){
 }
for(int i=0; i<N; i++)
    for (auto it = var.begin(); it != var.end(); ++it) {
	
    }


for (i = 0; i < N; ++i) {
    
 }
